export default function transformRawUserTransactions(rawUserTransactions: RetrievedUserTransactions): TransactionResponse {
	try {
		return rawUserTransactions?.purchase_orders.
	} catch (error) {
		console.error(error)
		throw Error
	}
}
